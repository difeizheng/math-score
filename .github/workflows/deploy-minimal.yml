name: Minimal Deploy Test

on:
  push:
    branches: [ master ]

jobs:
  test-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - run: echo "Test deployment successful"
